// Azure Resource Graph Query
//description: Returns a list of AKS clusters with Local Accounts disabled
Resources
| where type == "microsoft.containerservice/managedclusters"
| extend DisableLocalAccount = tostring(parse_json(properties.disableLocalAccounts))
| project DisableLocalAccount
